# Dependencias SOLO del API + ETL (imagen Docker liviana).
# No incluye la pila pesada de ML (torch/xgboost/lightgbm/sklearn): el API solo
# LEE de la base de datos y de los JSON de métricas; el ETL usa pandas/sqlalchemy.
# Rangos amplios para asegurar wheels disponibles en python:3.12-slim.

fastapi>=0.115
uvicorn>=0.30
pandas>=2.2
numpy>=1.26
pyarrow>=14
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9
python-dotenv>=1.0      # carga .env en local; no-op en despliegue
